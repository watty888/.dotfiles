# AUR packages. Consumed by restore.sh via paru.
#
# Gaming moved to Windows, so proton-ge-custom-bin / protonup-qt / xivlauncher
# are deliberately gone from this list. Reinstate them only if you go back to
# playing on Linux.
#
# Credentialed apps (anydesk-bin, ngrok) stay out on purpose — they need auth
# set up by hand after install.
#
# nvidia-580xx-dkms is NOT here; it is opt-in via `./restore.sh --with-nvidia`,
# and only needed if the HDMI port turns out to be wired to the dGPU.

brave-bin
cursor-bin
bruno
informant
